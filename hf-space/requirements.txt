# CPU-only PyTorch wheels — the free Space has no GPU, and the default CUDA
# build of torch is ~2.5 GB and blows the build. The +cpu wheels are small.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.2+cpu
torchaudio==2.1.2+cpu
# Demucs separator + audio I/O + the Gradio UI/API.
demucs==4.0.1
soundfile>=0.12
gradio==4.44.0
