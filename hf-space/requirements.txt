# CPU-only PyTorch wheels (no GPU on the free Space; the CUDA build is ~2.5 GB).
# torch 2.3.1 supports numpy 2, which gradio 6 pulls in; torchaudio 2.3 still has
# the built-in audio loader (newer torchaudio offloads to torchcodec).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchaudio==2.3.1+cpu
# Demucs separator + audio I/O.
demucs==4.0.1
soundfile>=0.12
# gradio 6 — 4.44 had a schema bug (bool in API schema) that crashed the app at
# runtime; 6.x fixes it and its bundled gradio_client matches.
gradio==6.18.0
