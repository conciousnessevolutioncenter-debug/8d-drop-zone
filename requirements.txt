# Vercel / serverless deployment — lightweight serving layer only.
# For local development with full DSP stack, use: pip install -r requirements-local.txt
fastapi>=0.100
uvicorn[standard]>=0.23
python-multipart>=0.0.7
