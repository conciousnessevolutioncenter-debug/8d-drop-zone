# Core server
fastapi>=0.100
uvicorn[standard]>=0.23
python-multipart>=0.0.7
# DSP stack — enables full spatial mastering on Railway and local servers.
# scipy and soundfile are optional but recommended; the engine falls back
# gracefully to numpy-only crossover filters and stdlib WAV if they are absent.
numpy>=1.24
scipy>=1.10
soundfile>=0.12
pydub>=0.25
# Optional: hosted AI stem separation. Tiny API clients, no torch.
#  - Replicate (paid per-use): activates when REPLICATE_API_TOKEN is set.
#  - Hugging Face Space (free 16 GB tier): activates when HF_SPACE_ID is set.
# Both fall back to the classic full-mix render when unset/unavailable.
replicate>=0.25
gradio_client>=1.0
