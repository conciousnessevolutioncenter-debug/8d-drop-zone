# Core server
fastapi>=0.100
uvicorn[standard]>=0.23
python-multipart>=0.0.7
# DSP stack — enables full spatial mastering on Railway and local servers.
# scipy and soundfile are optional but recommended; the engine falls back
# gracefully to numpy-only crossover filters and stdlib WAV if they are absent.
numpy>=1.24
scipy>=1.10
soundfile>=0.12
pydub>=0.25
# Optional: hosted AI stem separation (Demucs on Replicate). Tiny API client,
# no torch. Activates only when REPLICATE_API_TOKEN is set; otherwise the app
# falls back to the classic full-mix render.
replicate>=0.25
